# TRB review — `chatlab` v1.0.0 (GA gate)

- **Date:** 2026-04-30
- **Project:** `chatlab` v1.0.0 (GA candidate, package.json bumped, CHANGELOG dated, capability specs flipped to `Implemented`).
- **Panel:** the same 14 specialists as the rc-1 review on the same date.
- **Scope:** the GA candidate state immediately before tagging. **Follow-up to** [`2026-04-30-v1.0.0-rc.1.md`](./2026-04-30-v1.0.0-rc.1.md), specifically charged with verifying item 7 closure and re-baselining the maturity score.

## Reviewer note

This is the **GA review** the rc review explicitly required ("A second TRB review is convened before tagging `v1.0.0`"). The pareceres below are condensed updates against the rc snapshot — same panel, same severity scale, what changed in the four hours since the rc review.

## Executive summary

| Item | rc-1 (morning) | GA (afternoon) | Δ |
| --- | :---: | :---: | --- |
| **Maturity score** | 7.0 / 10 | **7.6 / 10** | +0.6 |
| **GA blockers (items 1–5)** | All Closed | All Closed | unchanged |
| **v1.0 soft items (6–10)** | 6, 8, 9, 10 Closed; 7 Partial | 6, 8, 9, 10 Closed; **7 Partial** (contrast check shipped, manual axe still owed) | unchanged |
| **Critical challenge (rc)** | Narrative + operational guarantees gap | **Largely addressed** — narrative section in README/home, `custom` provider as the wedge anchor, encryption + sweep + logger + banner all live | resolved |
| **New critical challenge (GA)** | n/a | **Three contrast findings from item 7's contrast-check sub-task** — `--warn`/`--danger`/`--ink-3-on-sunken` below AA. CSS-only fix. | new |
| **Readiness** | Proceed conditional on items 1–5 | **Proceed** with one of two paths: fix the 3 contrast findings before tag, or tag with knowns documented. | conditional |

The score rise reflects: 5 GA blockers genuinely closed (encryption, retention, logger, narrative, banner); UAT panel validated the wedge across 6 downstream roles; the `custom` provider lands as the headline. The score doesn't go higher because item 7's manual axe pass is still owed and the new contrast findings are real.

## Updated pareceres (compact deltas)

Pareceres unchanged from the rc review are not repeated; only changed material is below.

### 1. Product Manager — Δ +1.0

The "Why chatlab and not <X>?" comparison table landed in README + `docs/project-overview.md` (LangSmith / Promptfoo / OpenAI Playground); the `custom` provider became a first-class entry across UI dropdown, provider docs, cookbook, README phrasing. The UAT panel of six downstream roles converged on "the wedge holds" within five minutes — the strongest external signal we have. **The rc-1 critical challenge (weak external narrative) is closed.**

### 3. Security Engineer — Δ +0.7

`src/lib/{crypto,master-key}.ts` shipped with `test/storage/encryption.test.ts` exercising round-trip + legacy-plaintext migration. AES-256-GCM, 32-byte master key from env or `$CHATLAB_HOME/master.key` (mode 0600). Plaintext-legacy rows pass through readable; new writes encrypt under the active key. The findings of "API keys plaintext on disk" (Crítica) and "no rate limit" (the latter still open) are now: encryption Closed, rate-limit deferred to v1.1 by design. **Severity downgraded** from Crítica to Média (no plaintext, no public network exposure by default).

### 4. UI/UX Designer — Δ unchanged

ARIA fixes shipped (DevDrawer `role="log"`/`aria-live`, AnnotationsPanel `aria-controls`, ChatList `role="list"`/`"listitem"`/`aria-current`). The OKLCH contrast check (this morning's [companion artefact](./2026-04-30-axe-contrast-check.md)) caught **three real findings**: `--warn` on `--warn-bg` at 2.59:1 (fails AA), `--danger` on `--danger-bg` at 3.96:1 (AA Large only), `--ink-3` on `--bg-sunken` at 4.45:1 (just below AA). All CSS-only. Item 7 stays **Partial** until either the tokens are adjusted *or* manual axe runs against the live UI confirms no other findings. **Severity: Média (Alta if WCAG ambition is real for v1.0.0 GA).**

### 6. SRE — Δ +0.5

pino is wired into `Core` with TTY/JSON detection; `runRetentionSweep` emits structured logs per workspace; the boot path constructs the logger off `CHATLAB_LOG_LEVEL`. The earlier "zero structured observability" finding is downgraded — there's a log surface now, even if `/metrics` and OTel still aren't in. Risk drops from Média (local) / Alta (shared) to **Baixa / Média**.

### 7. Data Engineer — Δ +0.5

`Core.startRetentionSweep` runs on a 24h timer; the documented LGPD posture and the code now agree. The `agent_version` derivation concern (rename / model change losing prior attribution) wasn't addressed in this rc-to-GA window — flagged as v1.1. Severity drops from Alta to **Baixa**.

### 8. ML Engineer / AI Specialist — Δ +0.3

Capability `0007-eval-harness` was drafted with full Behavior section + acceptance criteria; ROADMAP v1.1 leads with it. UAT panel's ML engineer rated `0007` as the single most-anticipated v1.1 deliverable. Implementation still owed. The other findings (no temperature knob, no streaming, no token/cost in export) are unchanged — UAT items 1, 2, 10 group them into a single `schema_version: 2` bump for v1.1.

### 11. QA Engineer — Δ +0.3

`test/agents/runner-swap.test.ts` (RUN-SWAP-01) closed the workspace-swap-during-inflight finding from the rc review (Engineering item 6). E2E skeleton at `test/e2e/` opt-in via `CHATLAB_TEST_E2E=1`; storage benchmarks scaffold at `test/perf/storage-bench.test.ts` opt-in via `CHATLAB_TEST_PERF=1`. Test count: **90 passed | 2 skipped (92)**, up from 82+1 at rc-1. Coverage gate 80/80/80/65 still green.

### 14. Legal Advisor — Δ +0.3

Privacy banner shipped on both surfaces (CLI startup line when `agents.list()` returns ≥ 1 cloud-provider; UI dismissable banner via `sessionStorage`). DPA template updated to reflect at-rest encryption. The "six sub-processors" finding is unchanged — chatlab still doesn't enforce per-conversation consent or redaction, only surface-warns. Severity drops from Alta to **Média**.

## New finding (GA-only)

**Three CSS-token contrast findings** caught by the OKLCH contrast check ([artefact](./2026-04-30-axe-contrast-check.md)):

| Token | Issue | Severity |
| --- | --- | --- |
| `--warn` on `--warn-bg` | 2.59:1 — fails AA (text + UI) | **Média** |
| `--danger` on `--danger-bg` | 3.96:1 — passes AA Large only | **Baixa** |
| `--ink-3` on `--bg-sunken` | 4.45:1 — 0.05 below AA | **Baixa** |

All three are CSS-only (single L value adjustment per token). Either fix them and rerun the contrast script + screenshot capture before tag, or document as known and schedule for `v1.0.1`. Recommendation: fix `--warn` (the only one that fails AA outright), accept the other two as documented knowns, ship.

## Critical challenge (GA)

The rc critical challenge ("the project knows what it is, but does not yet communicate or guarantee that") is **closed**. The new GA-specific challenge is **the manual axe-DevTools pass remains the last unverified item**, and three contrast findings discovered programmatically already prove the manual pass would catch real things. Either accept ~2 hours of design-review time pre-tag to fix the 3 findings + run axe live, or tag with knowns documented.

## Action register diff vs. rc-1 review

The full register lives in [`docs/reviews/2026-04-30-v1.0.0-rc.1.md`](./2026-04-30-v1.0.0-rc.1.md). Status changes since the rc review:

| # | Item | rc Status | GA Status |
| --- | --- | --- | --- |
| 1 | Encrypt API keys at rest | Open | **Closed** |
| 2 | Auto retention sweep | Open | **Closed** |
| 3 | Structured logger | Open | **Closed** |
| 4 | "Why chatlab" comparison | Open | **Closed** |
| 5 | Privacy banner | Open | **Closed** |
| 6 | Workspace-swap test | Open | **Closed** |
| 7 | Manual axe sweep | Open | **Partial** — contrast check shipped (3 findings), manual axe still owed |
| 8 | Pin redocly | Open | **Closed** |
| 9 | Acceptance section | Open | **Closed** |
| 10 | Sequence diagram | Open | **Closed** |
| 11 | Eval harness spec | Open | **Spec drafted** (target v1.1) |
| 12 | E2E spec | Open | **Skeleton** (opt-in) |
| 13 | Storage benchmarks | Open | **Skeleton** (opt-in) |
| 14 | Compose example | Open | **Closed** |

(Items 11–14 keep their target as v1.1, but the v1.0 work owed for them — drafting the spec for `0007`, scaffolding the E2E + perf folders, shipping the compose example — is done.)

## Readiness statement

> The panel finds `chatlab v1.0.0` **ready to tag** with one explicit residual: item 7's three contrast findings + a manual axe pass against the live UI. The recommended path is **fix `--warn` (the only outright-AA failure), accept the other two as documented knowns in `docs/troubleshooting.md`, run a 15-minute axe sweep on the five main screens, then tag**. If the maintainer wants to ship without the axe pass, the GA goes out under the same knowns and item 7 becomes the first issue against `v1.0.1` (CSS-only patch release).

## What happens after tag

- `release.yml` publishes `chatlab@1.0.0` to npm and `jvrmaia/chatlab:1.0.0` / `:1.0` / `:1` / `:latest` to Docker Hub on tag push.
- A GitHub Release is auto-created with notes pulled from `CHANGELOG.md`'s `[1.0.0]` section.
- The Docusaurus site at `https://jvrmaia.github.io/chatlab/` updates on the same push to `main` (separate workflow, `docs-deploy.yml`).
- The next TRB review fires when `v1.1` enters its own rc cycle.
